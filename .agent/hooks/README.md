# 🪝 UNIVERSAL AGENT HOOKS SPECIFICATION

This directory contains declarative, stack-agnostic hook definitions for AI Agent Harnesses.

- `hooks.json`: Declarative event hook rules (PreToolUse safety guard & PostToolUse native formatting).
